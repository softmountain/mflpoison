base_config: ucf101_fdmm_dtm_poison_0to1_defense.yaml
overrides:
  dataset:
    num_clients: 10
    partition_id: fold1-alpha1-smoke
  federation:
    rounds: 1
    pretrain_rounds: 1
    attack_rounds: 1
    clients_per_round: 10
    batch_size: 64
    patience: 1
  generator:
    epochs: 1
    options:
      max_batches: 1
      log_interval: 1
